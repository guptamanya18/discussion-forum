[project]
name = "api-gateway"
version = "0.1.0"
description = "API Gateway – single entry point routing requests to microservices"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.135.1",
    "pydantic-settings>=2.10.1",
    "uvicorn>=0.41.0",
    "httpx>=0.28.0",
    "redis>=5.0.0",
    "python-jose>=3.5.0",
]
